{
    "command": "sha256sum",
    "cases":
    [
        {
            "predicate": "default",
            "class": "non-pure",
            "inputs": ["stdin"],
            "outputs": ["stdout"]
        }
    ]
}
